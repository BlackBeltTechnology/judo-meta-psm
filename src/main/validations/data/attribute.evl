import "../../operations/data/_importData.eol";

context JUDOPSM!Attribute {

    // reserved attribute names: id    
    constraint ReservedAttributeName {
    	check: self.name <> "id"
    	message: "Name " + self.displayName() + " is reserved"
    }
}
