context JUDOPSM!NumericType {

    // scale > precision
	constraint PrecisionIsLowerOrEqualThanScale {
		check: self.scale > self.precision
		message: "Precision (" + self.precision + ") must be less than scale (" + self.scale + "): " + self.name
	}

	// precision >= 0
	constraint ValidPrecision {
		check: self.precision >= 0
		message: "Precision must be at least 0: " + self.name
	}
}