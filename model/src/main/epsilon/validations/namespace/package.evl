context Package {

	// package must have exactly one namespace
	constraint PackageHasNamespace {
		check: Namespace.all.select(n | n.packages.contains(self)).size() == 1
		message: "Package " + self.name + " must have exactly 1 namespace"
	}
}
